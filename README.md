# Installation

```
conda create -n pwc python=3.7 -y
conda activate pwc
conda install cython numpy python-gflags tqdm matplotlib ipython jupyter jupyterlab -y
conda install -c anaconda cudatoolkit tensorflow-gpu=1.13.1 keras=2.2.4 -y
pip install opencv-python
```
